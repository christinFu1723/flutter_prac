import 'package:flutter/material.dart';

class AppTheme {
  /// 主题色
  static const Color primaryColor = Color(0xFF1C4899);

  static const Color backgroundDefaultColor = Color(0xFFF5F8F8);

  /// 次要颜色
  static const Color secondColor = Color(0xFF00B3BF);

  /// 主要背景色
  static const Color backgroundColor = Color(0xFFF5F6F8);

  /// TabBar 背景色
  static const Color tabBarBackground = Color(0xFFFFFFFF);

  /// 特殊操作栏背景色
  static const Color bottomBarBackground = Color(0xFF44D7B6);

  /// 特殊操作栏前景色
  static const Color bottomBarForeground = Color(0xFFFFFFFF);

  /// 情境配色 - 成功
  static const Color sitSuccessColor = Color(0xFF2CB630);

  /// 情境配色 - 失败
  static const Color sitDangerColor = Color(0xFFFF3C3C);

  /// 情境配色 - 警告
  static const Color sitWarningColor = Color(0xFFFFAD43);

  /// 情境配色 - 信息
  static const Color sitInfoColor = Color(0xFF87A1FD);

  /// 主要文本色
  static const Color textColor = Color(0xFF494867);

  /// 次要文本色
  static const Color secondTextColor = Color(0xFF4C5268);

  /// 输入框placeholder颜色
  static const Color placeholderColor = Color(0xff8E9B9B);

  /// 较亮文本色
  static const Color lightTextColor = Color(0xFFB1B6BC);

  /// 金额文本色
  static const Color amountColor = Color(0xFFF92222);

  /// 边框色
  static const Color borderColor = Color(0xFFE0E3E9);

  /// 列表常规箭头颜色
  static const Color tileArrowColor = Color(0xFFEEEFF2);

  /// 列表常规箭头尺寸
  static const double tileArrowSize = 16.0;

  /// 名称字号，一般用于标注业务名称
  static const double fontSizeName = 20.0;

  /// 标题字号
  static const double fontSizeTitle = 16.0;

  /// 正文字号
  static const double fontSizeBase = 16.0;

  /// 次要文本字号
  static const double fontSizeSecond = 14.0;

  /// 极小备注字号
  static const double fontSizeSmall = 12.0;

  /// 超极小备注字号
  static const double fontSizeMini = 10.0;

  /// 元素默认间距
  static const double gutter = 16.0;
}
